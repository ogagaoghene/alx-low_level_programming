#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 *@a : variable
 *@b : variable
 * Return: 0 or 1 .
 */
int mul(int a, int b)
{
	int result;
	result = a * b;
	return (result);
}
